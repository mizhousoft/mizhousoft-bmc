import { createFromIconfontCN } from '@ant-design/icons';
import iconfontUrl from '@/assets/iconfont/iconfont';

const FontIcon = createFromIconfontCN({
    scriptUrl: iconfontUrl,
});

export default FontIcon;
